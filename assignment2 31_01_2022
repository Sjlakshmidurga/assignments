#include <stdio.h>
int main() 
{
    int arr[] = {9,7,0,3,0,4,5,2};
    int i,count=0,n=8;
   for(i=0;i<n;i++) 
   {
       if(arr[i]>0)
       {
         arr[count++]=arr[i];
       }
       
   }
   while(count<n)
   {
       arr[count++]=0;
   }
   for(i=0;i<n;i++) 
   {
       printf(" %d ",arr[i]);
   }
   return 0;
}
